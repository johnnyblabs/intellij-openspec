version: "1.0"

ide: QDJVM

include:
  - name: All

profile:
  name: qodana.recommended

exclude:
  - name: All
    paths:
      - build
      - .gradle
      - openspec
      - scripts
      - docs

failThreshold: 0

baseline: qodana-baseline.sarif.json
