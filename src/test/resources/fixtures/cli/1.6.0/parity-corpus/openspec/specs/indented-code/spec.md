# Indented Code

## Purpose
A capability whose body sits beside a four-space indented code sample the parser must ignore.

## Requirements

### Requirement: Indented sample
The system SHALL process the input.

    #### Scenario: only an indented example
    The system SHALL never actually do this.

#### Scenario: Real one
- **WHEN** x
- **THEN** y
