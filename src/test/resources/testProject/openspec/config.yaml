schema: spec-driven
version: "1.2.0"

profile:
  name: TestProject
  description: A test project for integration testing
  language: Java 21

context: "Integration test context"

rules:
  testing: "All tests SHALL pass"
